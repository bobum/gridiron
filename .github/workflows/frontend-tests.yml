name: Frontend Tests

on:
  pull_request:
    branches: [ master ]
    paths:
      - 'gridiron-web/**'
      - '.github/workflows/frontend-tests.yml'
  push:
    branches: [ master ]
    paths:
      - 'gridiron-web/**'
      - '.github/workflows/frontend-tests.yml'
  workflow_dispatch:

jobs:
  component-tests:
    name: Component & Integration Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./gridiron-web

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: gridiron-web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Run component and integration tests
        run: npm test -- --run

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-component
          path: gridiron-web/coverage/
          retention-days: 7

  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    needs: component-tests

    services:
      sqlserver:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          ACCEPT_EULA: Y
          MSSQL_SA_PASSWORD: YourStrong!Passw0rd
          MSSQL_PID: Developer
        ports:
          - 1433:1433
        options: >-
          --health-cmd "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P YourStrong!Passw0rd -C -Q 'SELECT 1' || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 30s

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: gridiron-web/package-lock.json

      - name: Restore .NET dependencies
        run: dotnet restore

      - name: Install EF Core tools
        run: dotnet tool install --global dotnet-ef --version 8.0.*

      - name: Build API
        run: dotnet build Gridiron.WebApi/Gridiron.WebApi.csproj --no-restore

      - name: Update database with migrations
        working-directory: ./DataAccessLayer
        run: dotnet ef database update --project . --startup-project ../Gridiron.WebApi --connection "Server=localhost;Database=GridironCI;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;"

      - name: Seed test data
        working-directory: ./Gridiron.WebApi
        env:
          ConnectionStrings__DefaultConnection: 'Server=localhost;Database=GridironCI;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;'
        run: dotnet run -- --seed --force

      - name: Start API in background
        working-directory: ./Gridiron.WebApi
        env:
          ConnectionStrings__GridironDb: 'Server=localhost;Database=GridironCI;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;'
          ASPNETCORE_URLS: 'http://localhost:5000'
          E2E_TEST_MODE: 'true'
        run: |
          dotnet run &
          echo "API started with PID $!"
          sleep 10

      - name: Verify API is running
        run: |
          curl --retry 5 --retry-delay 2 --retry-connrefused http://localhost:5000/swagger/index.html || exit 1

      - name: Verify API has teams data
        run: |
          echo "Fetching teams from API..."
          curl -v http://localhost:5000/api/teams

      - name: Install frontend dependencies
        working-directory: ./gridiron-web
        run: npm ci

      - name: Install Playwright browsers
        working-directory: ./gridiron-web
        run: npx playwright install chromium --with-deps

      - name: Create .env file with E2E test mode
        working-directory: ./gridiron-web
        run: |
          cat > .env <<EOF
          # Azure Entra ID (External CIAM) Configuration
          VITE_AZURE_CLIENT_ID=29348959-a014-4550-b3c3-044585c83f0a
          VITE_AZURE_AUTHORITY=https://gtggridiron.ciamlogin.com/gtggridiron.onmicrosoft.com
          VITE_AZURE_REDIRECT_URI=http://localhost:3000
          VITE_AZURE_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

          # API Configuration
          VITE_API_URL=/api

          # E2E Test Mode
          VITE_E2E_TEST_MODE=true
          EOF
          echo "✓ Created .env file with E2E test mode enabled"
          cat .env

      - name: Verify Playwright config has test mode
        working-directory: ./gridiron-web
        run: |
          echo "Checking Playwright configuration for E2E test mode..."
          grep -A 5 "webServer:" playwright.config.ts
          echo "✓ Test mode should be enabled in webServer.env"

      - name: Run E2E tests
        working-directory: ./gridiron-web
        run: npm run test:e2e

      - name: Upload E2E test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: gridiron-web/playwright-report/
          retention-days: 7

      - name: Upload E2E test traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: gridiron-web/test-results/
          retention-days: 7
